# Backend environment configuration (example)
# Copy this file to .env and fill actual secrets

# MongoDB Atlas
# Replace <db_password> with your real password
MONGO_URI=mongodb+srv://hmhglobalecom:<db_password>@cluster0.xqqenec.mongodb.net/hmh-global?retryWrites=true&w=majority&appName=Cluster0

# Server
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# SMTP (example placeholders)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=youremail@example.com
SMTP_PASS=your_app_password
EMAIL_FROM="HMH Global <no-reply@hmhglobal.co.uk>"
